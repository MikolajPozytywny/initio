/**
 * @interface User
 */
export interface User {
  filters: any;
  email: string;
  name: string;
  avatar_url: string;
  id: string;
  contacts: string[];
  description: string;
  PeopleSeen: string[];
}
